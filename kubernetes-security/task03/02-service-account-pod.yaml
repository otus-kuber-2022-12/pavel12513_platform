---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: jane
  namespace: dev

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: dev
  name: admin
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "watch", "list"]
---


apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: RoleBinding-dev # Под этим именем увидим созданный RoleBinding
  namespace: dev
roleRef:
  apiGroup: ""
  kind: Role # К чему привязываемся
  name: admin # имя Role иди ClusterRole к котором мы биндимся
subjects:
  - apiGroup: ""
    kind: ServiceAccount # Указываем к чему привязываемся Role, ServiceAccount
    name: system:serviceaccounts:dev
    namespace: dev